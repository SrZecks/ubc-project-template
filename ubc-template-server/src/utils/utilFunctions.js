const functions = {
    'formatStringLower': (text) => {
        return String(text).toLowerCase().trim()
    },
    'formatStringUpper': (text) => {
        return String(text).toUpperCase().trim()
    },
}

module.exports = functions